#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    cin.tie(NULL); ios_base::sync_with_stdio(false);
    ll n, a;
	cin >> n;
	for(int i=0; i<n; i++) {
		cin >> a;
		cout << a << " " << a << "\n";
	}
} 