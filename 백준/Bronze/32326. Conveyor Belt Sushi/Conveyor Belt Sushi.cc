#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    cin.tie(NULL); ios_base::sync_with_stdio(false);
    ll r, g, b;
	cin >> r >> g >> b;
	cout << (r*3 + g*4 + b*5) << "\n";
    return 0;
} 