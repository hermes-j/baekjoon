#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    cin.tie(NULL); ios_base::sync_with_stdio(false);
    string guho;
	cin >> guho;
	if (guho == "SONGDO") {
		cout << "HIGHSCHOOL\n";
	} else if (guho == "CODE") {
		cout << "MASTER\n";
	} else if (guho == "2023") {
		cout << "0611\n";
	} else if (guho == "ALGORITHM") {
		cout << "CONTEST\n";
	}
    return 0;
} 