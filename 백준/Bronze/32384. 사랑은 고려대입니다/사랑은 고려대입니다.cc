#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main() {
    cin.tie(NULL); ios_base::sync_with_stdio(false);
    ll n;
	cin >> n;
	for(int i=0; i<n; i++) {
		cout << "LoveisKoreaUniversity" << " ";
	}
    return 0;
} 